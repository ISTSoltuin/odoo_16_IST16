<odoo>
    <template id="product_template_inherit_fandi_api" inherit_id="website_sale.product">
        <xpath expr="//*[@id='add_to_cart_wrap']" position="after">
            <div id="simulate_wrap" class="d-inline-flex align-items-center mb-2 me-auto">
                <a id="simulate_button" t-attf-class="btn btn-secondary #{'btn-lg' if ctaSizeBig else ''}" href="#">
                    <i class="fa fa-calculator me-2"/>
                    <span style="font-weight: bold">Simule</span>
                </a>
            </div>
        </xpath>
        <xpath expr="//*[@id='o_wsale_cta_wrapper']" position="after">
            <div class="font-monospace pt8 bg-light o_default_snippet_text">
                <div class="container" id="formSimulacaoContainer" style="display: none;">
    
                    <form id="formSimulacao">
                        <div class="form-group">
                            <label for="nome">Nome*</label>
                            <input type="text" id="name" name="nome" class="form-control"
                                required="required" />
                        </div>

                        <div class="form-group">
                            <label for="cpf">CPF* (somente os dígitos)</label>
                            <input type="text" id="cpf" name="cpf" class="form-control"
                                required="required" />
                        </div>

                        <div class="form-group">
                            <label for="telefone">Telefone</label>
                            <input type="text" id="telefone" name="telefone" class="form-control"
                                required="required" />
                        </div>

                        <button type="button" id="btnSimular" class="btn btn-primary" t-on-click="toggleFormVisibility">Simular
                            Financiamento</button>
                    </form>
                </div>
                <div id="resultadoSimulacao" class="card shadow-lg p-3 mb-5 bg-white rounded" 
                    style="max-width: 600px; display: none;">
                    <div class="card-body text-center">
                        <h5 class="fw-bold">Que ótimo, <span id="simulacaoNome"></span>!</h5>
                        <p>Encontramos a seguinte condição para o seu perfil:</p>
                        <div class="border rounded p-3 bg-light">
                            <h6 class="text-muted">CHEVROLET</h6>
                            <h4 class="fw-bold text-primary">CRUZE</h4>
                            <p class="mb-0">Valor do Veículo: 
                                <strong>R$ 
                                    <span id="valorVeiculo"></span>
                                </strong>
                            </p>
                            <p>Entrada: 
                                <strong>
                                    <span id="percentualEntrada"></span>%
                                </strong>
                            </p>
                        </div>
                        <div class="mt-3">
                            <h6>Parcelas</h6>
                            <h3 class="fw-bold text-success"><span id="parcelas"></span>x R$ <span id="valorParcela"></span></h3>
                        </div>
                        <div class="mt-3">
                            <h3 class="fw-bold text-success">
                                <button type="button" class="btn btn-primary" id="linkRedirecionamento"></button>
                            </h3>
                        </div>
                        <p class="small text-muted mt-2">
                            *As condições apresentadas não são garantia de crédito, pois dependem de análise da instituição financeira concedente.
                        </p>
                    </div>
                </div>
            </div>
        </xpath>
    </template>
</odoo> 
